syntax = 'proto3';

import "{{name}}.message.proto";

package hive.{{name}}.v1;

service {{pascalCase name}} {
  // TODO: Add your RPC methods here.
  // Example:
  // rpc QueryItems(QueryItemRequest) returns (QueryItemResponse){};
  // rpc GetItem(GetItemRequest) returns (GetItemResponse){};
  // rpc CreateItem(CreateItemRequest) returns (GetItemResponse){};
  // rpc UpdateItem(UpdateItemRequest) returns (GetItemResponse){};
  // rpc DeleteItem(DeleteItemRequest) returns (GetItemResponse){};
}
