{{#if includeDatabase}}# PostgreSQL — {{name}} domain schema
DATABASE_URL=postgresql://user:password@localhost:5432/hive_{{name}}

{{/if}}# HTTP port for registry health endpoint (0 = auto-assign a free port)
PORT=0
{{#if includeGrpc}}
# gRPC server port (0 = auto-assign a free port)
{{constantCase name}}_GRPC_SERVICE_PORT=0
{{/if}}
# Registry service address (default: 0.0.0.0:4001)
# SERVER_URL=0.0.0.0:4001
